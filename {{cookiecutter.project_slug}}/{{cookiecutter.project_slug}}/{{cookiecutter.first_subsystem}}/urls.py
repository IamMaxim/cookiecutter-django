from django.urls import path

# Use this file to define Django template urls for your app.
# For DRF views/viewsets, proceed to api/api_router.py.

app_name = "{{ cookiecutter.first_subsystem }}"
urlpatterns = [
    # path("path/", view=your_view, name="basename"),
]
